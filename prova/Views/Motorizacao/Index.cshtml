@model IEnumerable<prova.Models.Motorizacao>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Modelo)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Modelo)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.MotorizacaoId }) |
                @Html.ActionLink("Details", "Details", new { id=item.MotorizacaoId }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.MotorizacaoId })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
